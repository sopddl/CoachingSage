// Views/Screens/SessionView.swift
// Story 1.2 placeholder — contenu Epic 3+ (séance active, exercices, timer).
import SwiftUI

struct SessionView: View {
    var body: some View {
        PlaceholderScreen(
            systemImage: "figure.gymnastics",
            titleKey: "tab.session",
            subtitleKey: "placeholder.coming_soon"
        )
    }
}

#Preview {
    SessionView()
}
