import SwiftUI

// Placeholder Story 1.1a.1 — implémentation complète en Story 1.1a.3
// (SwiftData ModelContainer + AppDependencies + AuthView switch).

@main
struct CoachingSageApp: App {
    var body: some Scene {
        WindowGroup {
            Text("CoachingSage — bootstrap Epic 1")
                .padding()
        }
    }
}
