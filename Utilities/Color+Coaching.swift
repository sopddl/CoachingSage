// Utilities/Color+Coaching.swift
// Design tokens couleur CoachingSage — placeholder Story 1.1a.
// ⚠️ Story 1.2 remplacera par la palette finale (Color+Coaching via xcassets avec dark mode).
import SwiftUI

extension Color {

    // MARK: - Couleur signature (placeholder)
    /// Action principale, icône app — remplacé en Story 1.2
    static let coachingPrimary = Color.accentColor

    // MARK: - Texte
    /// Texte principal — remplacé en Story 1.2
    static let coachingTextPrimary = Color.white
    /// Texte secondaire — remplacé en Story 1.2
    static let coachingTextSecondary = Color.secondary

    // MARK: - États sémantiques (placeholder)
    static let coachingError = Color.red
    static let coachingWarning = Color.orange
    static let coachingSuccess = Color.green
    static let coachingDisabled = Color.gray

    // MARK: - Fonds (placeholder)
    static let coachingBackground = Color(.systemBackground)
    static let coachingCard = Color(.secondarySystemBackground)

    // MARK: - Accent / signature assistant Léon (placeholder)
    static let coachingLeon = Color.blue
    static let coachingLeonText = Color.white
}
